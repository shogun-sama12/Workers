server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # SPA routing: if file not found, serve index.html
        try_files $uri $uri/ /index.html;
    }
}
